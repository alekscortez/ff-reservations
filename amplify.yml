version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 22 && nvm use 22
        # The lock file is generated by npm 11.6.2 (declared in package.json's
        # `packageManager` field). Node 22 ships with npm 10.9.7, whose `npm
        # ci` validation rejects the npm-11-written lock file with
        # "Missing chokidar@4.0.3 from lock file" even though the entry is
        # present. Corepack reads `packageManager` and activates the right
        # npm version, so `npm ci` succeeds the way it does locally.
        - corepack enable
        - corepack prepare npm@11.6.2 --activate
        - node --version
        - npm --version
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/ff-reservations/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
