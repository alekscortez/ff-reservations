version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install && nvm use
        - node --version
        - npm --version
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/ff-reservations/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
