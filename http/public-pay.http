### Cash App Link (Staff: create session link)
POST {{baseUrl}}/reservations/{{reservationId}}/cashapp-link/square
Authorization: Bearer {{accessToken}}
Content-Type: application/json

{
  "eventDate": "{{eventDate}}",
  "ttlMinutes": 20
}

###
### Cash App Session (Client page bootstrap)
GET {{baseUrl}}/cashapp/session?eventDate={{eventDate}}&reservationId={{reservationId}}&token={{cashAppToken}}

###
### Cash App Charge (Client completes payment)
POST {{baseUrl}}/cashapp/session/charge
Content-Type: application/json

{
  "eventDate": "{{eventDate}}",
  "reservationId": "{{reservationId}}",
  "token": "{{cashAppToken}}",
  "sourceId": "{{squareSourceId}}"
}
